# Data directory (re-downloaded at runtime)
data/

# Docker local override
docker-compose.override.yml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environment
venv/
ENV/
.venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# Testing
.tox/
.coverage
.pytest_cache/
htmlcov/
.nox/

# Logs
logs/
*.log

# Database
*.db
*.sqlite3

# OS
.DS_Store
Thumbs.db

# Configuration (secrets)
config/config.yaml
config/config.yml
config/azure.yaml
.env
.env.local
.env.*.local

# Alembic
alembic/versions/*.pyc

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Jupyter
.ipynb_checkpoints/

# Dev tracker (local issue tracking)
dev-tracker/

# Stress test results & reports (large CSV logs, product lists)
tests/stress_test/

# Local design notes / project memory
memory/

# Local benchmark / diagnostic directory — entirely local, never tracked.
# The functionality of the standalone scripts is now exposed as proper
# CLI commands in the main package: `download-icecat-index` and
# `prefilter-report`. The benchmark/ directory is for local-only
# experiments and can hold whatever you need.
benchmark/

# Internal-only drafts, correspondence, scratch notes — kept on disk but
# never committed (everything goes inside this directory).
internal/
